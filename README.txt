Telegram-бот для учёта строительных инструментов
Описание:
Этот Telegram-бот создан для автоматизации учёта и перемещения строительных инструментов между объектами. Бот поддерживает работу с QR-кодами, создаёт заявки на перемещение, обрабатывает одобрение/отклонение заявок и экспортирует их в XML-формате, имитируя интеграцию с 1С.

Стек технологий:

Python 3.10+

Aiogram 3.x

SQLite

Pillow + pyzbar — для распознавания QR-кодов с изображений

Как запустить проект

Установите зависимости:
pip install aiogram Pillow pyzbar

Добавьте токен Telegram-бота в bot.py:
bot = Bot(token="ВАШ_ТОКЕН")

Создайте базу данных и таблицы:
python create_db.py

Добавьте тестовые данные (инструменты и пользователей):
python seed_data.py

Запустите бота:
python bot.py

💬 Доступные команды бота:
/start	                        Регистрация пользователя в системе
(Фото с QR-кодом)	        Распознаёт QR-код, выводит данные об инструменте
/move_request QR123 Объект Б	Создаёт заявку на перемещение инструмента
/my_requests	                Показывает все заявки, сделанные текущим пользователем
/approve <id>	                Админская команда — одобрить заявку
/reject <id>	                Админская команда — отклонить заявку
/export_requests	        Сохраняет все заявки в XML-файл requests_export.xml

Структура проекта:

toolbot/
├── bot.py                # Основной логика Telegram-бота
├── create_db.py          # Скрипт для создания таблиц в базе
├── seed_data.py          # Тестовые данные: пользователи и инструменты
├── check_requests.py     # Просмотр всех заявок через консоль
├── tools.db              # SQLite база данных
├── requests_export.xml   # Экспорт заявок в XML (эмуляция 1С)
└── README.md             # Этот файл 🙂

Как проверить работу
Отправьте QR-код (например, QR123) в виде фото — бот распознает инструмент.

Выполните команду:
/move_request QR123 Объект Б
Проверьте /my_requests
Выполните /approve 1 или /reject 1В
Экспортируйте заявки: /export_requests